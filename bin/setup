#!/usr/bin/env ruby
# frozen_string_literal: true

# Installs all dependencies specified in the Gemfile

require 'bundler/setup'

# Require any dependencies from your Gemfile
Bundler.require

puts 'All dependencies have been successfully installed.'
