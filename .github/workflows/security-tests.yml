name: SecurityTests
on:
    workflow_call:
    workflow_dispatch:

jobs:
    test-and-build:
        runs-on: ubuntu-latest
        permissions:
            contents: read
        steps:
            - name: Checkout
              uses: actions/checkout@v4
              with:
                fetch-depth: 0
            - name: Setup Ruby
              uses: ruby/setup-ruby@v1
              with:
                  ruby-version: 3.0
                  bundler-cache: true
            - name: SAST(Talisman) - Detect secrets in commits
              uses: carhartl/talisman-secrets-scan-action@v1.4.0
            - name: Upload SAST(Talisman) Report
              uses: actions/upload-artifact@v4
              with:
                  name: talisman-report
                  path: ${{github.workspace}}/talisman_report
            - name: Dependency Check
              uses: dependency-check/Dependency-Check_Action@main
              with:
                  project: 'hello_world_hex_ruby'
                  path: '.'
                  format: 'HTML'
                  out: 'dependency-check'
            - name: Upload Dependency Check Report
              uses: actions/upload-artifact@v4
              with:
                  name: dependency-check
                  path: ${{github.workspace}}/dependency-check